# Scripts

This directory is reserved for diagnostic helper scripts.

The main implementation should live in the CLI, not in shell scripts. Scripts should help users inspect route state, egress IP, `sbx` availability, and cleanup behavior.
