# Issue Tracker

This repository uses GitHub Issues as the issue tracker.

Engineering skills that create or update work items should use the `gh` CLI against the repository remote. External pull requests are not treated as the default request intake surface for triage.

PRD issues should be labeled `ready-for-agent` when the request is already specified enough for an implementation agent to start without additional product discovery.
